apiVersion: v1
kind: ConfigMap
metadata:
  name: api-configmap
  namespace: default
data:
  NODE_ENV: production
  DB_USER: postgres
  DB_HOST: rangeiq-db
  DB_NAME: rangeiq
  DB_PASSWORD: docker
  DB_PORT: '5432'
